{
  "name": "bandrad-crm-api-Flow",
  "version": "1.0.0",
  "private": true,
  "description": "Beginner-friendly CRM to HDB portal automation",
  "scripts": {
    "env:dev": "node -e \"require('node:fs').copyFileSync('.env.dev','.env')\"",
    "env:stage": "node -e \"require('node:fs').copyFileSync('.env.stage','.env')\"",
    "env:prod": "node -e \"require('node:fs').copyFileSync('.env.prod','.env')\"",
    "debug:rv:dev": "npm run env:dev && playwright test tests/hdb/rvFlow.spec.js --headed --debug",
    "debug:ov:dev": "npm run env:dev && playwright test tests/hdb/ovFlow.spec.js --headed --debug",
    "start:hdb": "npm run env:prod && node scripts/runHdbVerificationWorker.js --headed",
    "debug:hdb": "npm run env:prod && node --inspect scripts/runHdbVerificationWorker.js --headed --debug",
    "debug:hdb:dev": "npm run env:dev && node --inspect scripts/runHdbVerificationWorker.js --headed --debug",
    "debug:hdb:dev:inspector": "npm run env:dev && set PWDEBUG=1&& node scripts/runHdbVerificationWorker.js --headed",
    "debug:hdb:stage": "npm run env:stage && node --inspect scripts/runHdbVerificationWorker.js --headed --debug",
    "test:hdb:worker": "node --test tests/hdb/worker",
    "update-status:dev": "npm run env:dev && playwright test tests/updateStatus.spec.js --headed",
    "update-status:stage": "npm run env:stage && playwright test tests/updateStatus.spec.js --headed",
    "report": "playwright show-report --port 9324",
    "report:file": "start playwright-report/index.html"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "dotenv": "^17.4.2",
    "playwright": "^1.55.0"
  },
  "dependencies": {
    "exceljs": "^4.4.0"
  }
}

node_modules/
output/
test-results/
playwright-report/
blob-report/

# Keep credentials local. Existing tracked environment files should be
# removed from Git history separately after their credentials are rotated.
.env
.env.*
!.env.example

# Local CRM response used only by the temporary image-preparation helper.
devhelpers/response.json
